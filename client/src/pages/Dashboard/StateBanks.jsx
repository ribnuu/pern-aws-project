import React from 'react'

const StateBanks = () => {
    return (
        <div>A detailed map that shows all the available ATMs island wide is shown. The suspects actvity and the usage of ATM is shown in a different colour. Date wise transactions at banks and ATM are also shown
        </div>
    )
}

export default StateBanks
