// posModelRegistry.js

const modelRegistry = {
  expenses: {
    allowedModels: ["ExpensesCategory", "ExpensesSubCategory"],
  },
  // Add more entities and their allowed models as needed
};

module.exports = {
  modelRegistry,
};
